{
  "name": "planningpoker",
  "version": "2.0.0",
  "description": "Mentally Friendly's Agile Planning Poker cards app",
  "private": true,
  "main": "src/server.js",
  "scripts": {
    "start": "NODE_ENV=dev docker-compose up",
    "start:production": "docker-compose up",
    "serve:production": "node ./src/server.js",
    "serve:dev": "npx nodemon ./src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "@liamfiddler",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^2.0.3"
  },
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^2.3.0"
  }
}
